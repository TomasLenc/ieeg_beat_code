# this imports all definitions from the img_pipe.py module in the current
# folder, so that we can call them directly using the dot notation:
# img_pipe.somefunction()
print('importing img_pipe...')
from .img_pipe import *
